# Seed Category Rules

1. Open `supabase/seed_category_rules.sql`.
2. Replace `00000000-0000-0000-0000-000000000000` with a real `auth.users.id`.
3. Run the SQL in the Supabase SQL editor.

The app also lets users add, edit, and delete these rules from Settings.
